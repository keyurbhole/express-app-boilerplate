
/**
 * Middleware that authenticates all APIs under 'apiRouter' Router
 * @param {*} req 
 * @param {*} res 
 * @param {Function} next 
 */
const middleware = (req, res, next) => {
    /**
     * write middlewares
     */
    next();
};


export { middleware }